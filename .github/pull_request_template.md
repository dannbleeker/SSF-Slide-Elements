## What changed

## Why

## How it was checked

- [ ] `npm run lint`, `npm run typecheck`, `npm test`
- [ ] If the library deck changed: `npm run build:lib && node scripts/harvest.mjs`, catalogue committed
- [ ] If the pane changed: looked at it at 320 and 512, in both themes
- [ ] If host behaviour is involved: says plainly what is measured and what is assumed
